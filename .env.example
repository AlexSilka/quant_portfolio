# Copy to .env and fill in. .env is gitignored — never commit real keys.
# Twelve Data API key — equities/FX bars (https://twelvedata.com). Required for `make reproduce`.
# Crypto (Binance bulk) and macro (FRED) need no key.
TWELVEDATA_API_KEY=

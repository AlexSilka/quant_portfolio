# Python 3.12. macOS: run `brew install libomp` before installing lightgbm.
# Pinned, full runtime stack (verified on Python 3.12). Fixed versions keep every
# reported number reproducible; see docs/ARCHITECTURE.md for what each group backs.

numpy==1.26.4
pandas==2.2.3
pyarrow==17.0.0

# data sources
pandas-market-calendars==5.4.0
requests==2.34.2

# modelling / stats (sleeve machinery)
scipy==1.14.1
scikit-learn==1.5.2
lightgbm==4.7.0

# validation
arch==8.0.0

# reporting
matplotlib==3.9.2
